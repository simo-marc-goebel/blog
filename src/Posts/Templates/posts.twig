<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ pHeadline | default('Blog Post') }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/assets/stylesheets/post_detail.css">
    <link rel="stylesheet" href="/assets/stylesheets/styles.css">
</head>
{% block body %}
<body>
<div id="navbar-placeholder"></div>
<div id="sidebar-placeholder"></div>

<button id="addPostBtn" class="addPostBtn" style="margin-left:80%; width:45px"><i class="bi bi-plus-square-dotted" data-bs-toggle="modal" data-bs-target="#postModal"></i></button>

<div class="postGrid">
    <?php foreach ($posts as $post): ?>
    <div class="postGridContentBox">
        <div class="postGridContentAuth">
            <p>Author: <?= htmlspecialchars($post['author_name'] ?? 'Unknown') ?></p>
            <p>Created: <?= date('d.m.Y', strtotime($post['createdAt'])) ?></p>
            <p>At: <?= date('H:i:s', strtotime($post['createdAt'])) ?></p>
        </div>
        <div class="postGridContentText">
            <h2><?= htmlspecialchars($post['headline']) ?></h2>
            <p><?= nl2br(htmlspecialchars($post['postContent'])) ?></p>
            <a href="https://simo-blog.test/index.php?page=postDetails&id=<?= urlencode($post['id']) ?>" class="btn btn-primary mt-2" style="height:25px;margin-bottom:5px;margin-left:80%"></a>
        </div>
        <div class="postGridContentImg">
            <img src="/assets/images/postPics/<?= htmlspecialchars($post['imgPath']) ?>" class="img-fluid" alt="">
        </div>
    </div>
    <?php endforeach; ?>
</div>
<br><br><br>

<div class="modal fade" id="postModal" tabindex="-1" aria-labelledby="postModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">

            <div class="modal-header">
                <h5 class="modal-title" id="postModalLabel">Add a Post</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>

            <div class="modal-body">
                <form id="postForm">
                    <div class="mb-3">
                        <label for="postHeadline" class="form-label">Your Post</label>
                        <textarea class="form-control" id="postHeadline" rows="1" maxlength="100" placeholder="Headline" required></textarea>
                    </div>
                    <div class="mb-3">
                        <label for="postInput" class="form-label">Your Post</label>
                        <textarea class="form-control" id="postInput" rows="4" maxlength="1500" placeholder="Write your post here..." required></textarea>
                    </div>
                    <div>
                        <select name="country" id="country" class="form-control">
                            <option value="">Loading countries...</option>
                        </select>
                    </div>
                    <br>
                    <div class="mb-2" id="uploadImgPost">
                        <label for="imageInput">Choose an image:</label>
                        <input type="file" name="postImage" id="imageInput" accept="image/*">
                    </div>
                </form>
            </div>

            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="submit" form="postForm" class="btn btn-primary">Submit</button>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
<script src="../assets/scripts/static.js" defer></script>
<script src="../assets/scripts/posts.js" defer></script>

</body>
{% endblock %}